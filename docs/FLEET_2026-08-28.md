# THE FLEET — six paper accounts, six mandates (2026-08-28, ~08:00 ET)

**Murat's instruction:** six new paper accounts replacing the old ones; one or
two safe, the rest maximising P&L with options, risky small-cap trades and the
future-state themes he believes in (robotics sensors/actuators, quantum,
nuclear, batteries/storage, grid/alt energy, raw materials). Everything learned
in a week of pre-market runs applied.

**Declaration lives in code:** `alpha/fleet.py`. `python -m scripts.fleet --plan`.

| role | tier | profile (per name / aggregate) | objective | universe | brains | one question |
|---|---|---|---|---|---|---|
| `hack1` (anchor) | SAFE | conservative 3% / 20% | median | SPY QQQ IWM (+NVDA AVGO PANW drift) | post_event_drift | does the attended beta book (+SPY ATM call 5%) hold +0.3% median? |
| `hack2` (drift) | SAFE | aggressive 8% / 50% | median | whole-market printers | post_event_drift | does the one positive-counterfactual brain (+1 open, +1.08%, t 2.82) pay at size? |
| `hack3` (thesis) | RISKY | **basket 6% / 80%** | median | 40 verified theme names | theme_basket | does the future-state basket beat IWM this week, bought after a 20-50% drawdown? |
| `hack4` (predator) | RISKY | maximum 15% / 75% | median | whole-market printers | post_event_drift (+pair) | does post-print continuation pay outside the mega-11 via shares and short-loser/long-IWM? |
| `hack5` (convexity) | RISKY | **convex 5% / 40%** | **mean** | 40 theme names with chains | theme_basket + drift | options only (long call / bull call spread), EV-ranked: where does chasing the mean end after five sessions? |
| `hack6` (council) | RISKY | aggressive 8% / 50% | median | whole-market printers | council_vector + drift | does a specialised-LLM thesis vector beat the price-only drift brain on the same printers? |

Every account keeps the HARD guards (paper host, genesis, no LLM order path,
`tif=day` options, bounded worst case). The RISKY tier changes SIZE, universe,
objective and structure kinds — never the guards.

## What was built for it (all tested, `tests_smoke_fleet.py`, 93 checks; suite green)

- `alpha/fleet.py` — mandates as data; `loop_args()`, `env_for()`,
  `railway_commands()`, `env_template()`.
- `scripts/fleet.py` — `--plan`, `--env-template`, `--railway <role>|all`,
  `--check` / `--check-all` (venue: FRESH vs LEGACY, equity, orders of any
  status, options level).
- `scripts/theme_screen.py` — 70 hand-curated theme names verified on the
  venue: **40 tradable & liquid, all 40 with chains**. Seed:
  `docs/seed/universe/THEMES_2026-08-28.json` (carries the survivorship caveat).
- `alpha/brains/theme_basket.py` — the human prior as a `direction` claim:
  +0.5σ tilt on the name's own realised vol; refuses names not in the seed,
  thin history, and falling knives (< −55% from the 20-session high).
  `evidence.measured_edge = None` on every row — it is labelled a prior.
- `alpha/brains/council_vector.py` — reads the day's council packet; refuses
  LIGHT/PARTIAL verdicts, non-independent skeptic, direction none, timing
  "quarters", p_already_priced > 0.7.
- `alpha/engine/sizing.py` — profiles `basket` (6%/80%/cap 1.0) and `convex`
  (5%/40%/cap 2.0); `GATED_PROFILES` need `AAT_ALLOW_MAXIMUM=1` before kickoff.
- `alpha/runner.py` — `AAT_STRUCTURE_KINDS` allowlist (applied after the share
  structure joins, before the claim matrix); chain refusal tolerated for ANY
  direction claim; `share_structure` reads spot from the stock snapshot when
  there is no chain.

## Three defects found by running the mandates, not by reading them

1. **A brand-new account cannot trade on day one.** Alpaca reports
   `last_equity=0` until the first close; the −3% daily latch read that as
   CANNOT DETERMINE and refused every entry (12/12 on staging). Every fleet
   account and the judged account are new on kickoff day. Fixed in
   `alpha/daybreak.py`: first session derives the drawdown against the
   **genesis** starting equity; no genesis → still refused, and the message
   names the missing step. **So `scripts.genesis --freeze` is mandatory per
   role before the first order** — `alpha/genesis.py` now accepts fleet roles
   (`--force` records a scratch account's true order count instead of refusing).
2. **A prior sized like an edge is a concentration bet.** Under `maximum` the
   basket put 25–34% of equity in each of the first three names. `basket`
   profile: 4.8% per name measured, 15+ names.
3. **Shares were dying on empty chains.** FLNC/NTLA have no contracts at the
   contest expiry; the direction forecast raised before the share structure was
   built. Now proceeds on stock quotes (`no chain at …; direction forecast
   proceeds`).

Dry pass under the `thesis` mandate (staging, pre-market quotes): 10 of 12
names produced a `long_shares` order at 4.8% each. The `convexity` dry pass
was cut short — the staging account went `ACCOUNT_CLOSED_PENDING` mid-run
(Murat replacing the accounts) and its key returns 401; the Railway staging
deployment was taken down so it does not cycle against a dead key.

## The number on the thesis account

The theme universe is **down 20–50% over the last 20 sessions** (OUST −50%,
AEVA −51%, IONQ −40%, BE −46%, FCEL −50%; median ≈ −30%) at 60–170%
annualised vol. Five sessions at 100% vol is a ~14% one-sigma swing. That is
the bet the human asked for, in his words "start investing early even though
it's a risk" — the account exists so the verdict is a number against IWM and
against `drift`, and the sizing is a basket so no single knife decides it.

## ADJUDICATED 09:40 ET — the thesis construction, on 12 years of CRSP

`aegis-finance/scripts/knife_basket_backtest.py` (receipt
`backend/data/optimus/knife_basket_2013_2024.json`): every session 2013-2024,
liquid names (>= $2, >= $2M/day) by 60-session vol band and 20-session
drawdown band, equal-weight, hold 5 sessions, non-overlapping windows, vs the
equal-weighted market.

| vol band | dd20 band | windows | excess/5d | median | hit | t | wealth vs mkt |
|---|---|---|---|---|---|---|---|
| 60–100% | −50..−30% | 477 | −0.30% | −0.25% | 48% | −1.60 | 0.20 vs 1.78 |
| 60–100% | −30..−20% | 585 | −0.32% | −0.35% | 45% | **−2.35** | 0.26 vs 3.20 |
| 100–180% | **< −50%** | 88 | **+2.32%** | **+2.72%** | **58%** | **+2.60** | **8.72 vs 1.85** |
| 100–180% | −50..−30% | 370 | +0.54% | −0.09% | 49% | 1.52 | 5.98 vs 2.41 |
| 100–180% | −30..−20% | 347 | −0.44% | −0.97% | 43% | −1.43 | 0.19 vs 2.02 |
| any | > −10% | 595 | −0.13% | −0.09% | 48% | −0.7 | flat |

The basket as first drawn (down 20–50%) is the losing construction; the
human's rule is right only at the extreme. `theme_basket` now buys the
rebound cell at full tilt, near-high names at half tilt, and DECLINES the
middle with the number. Today that makes `hack3` mostly cash (OUST at −50% is
the one candidate) — a typed human thesis (`scripts.thesis`) can still put a
name in, and is graded as such.

**Second pass (`scripts/knife_rebound_split.py`, 06:40 ET): the rebound cell
does not survive.** Its +2.32% needed ≥5 qualifying names per window, which
selects crisis months; at ≥3 names it is +0.19% mean, **−0.63% median, hit
46%, t 0.40, 0.75× vs 2.1×** (n=360), and no year (2014–2024), hold (1/3/5/10),
edge (−40/−50/−60%; rv 80/100/150%) or size tercile has a positive median.
**No drawdown cell of this construction pays at five sessions.** `theme_basket`
now buys only names near their 20-session high (half tilt, measured flat) and
declines every drawdown with the number. A five-session contest cannot
adjudicate a multi-year thesis; a typed human thesis can still put a name in.

## Kickoff sequence — DONE 28 Aug 08:30–09:10 ET, except the last line

```
python -m scripts.fleet --check-all           # six FRESH, $100,000, level 3     DONE
scripts.genesis --freeze --role hack1..hack6  # six birth records, seeded        DONE
python -m scripts.fleet --deploy all          # six Railway services + volumes + variables   DONE
python -m scripts.fleet --deploy all --up     # STARTS THE LIVE LOOPS  <- run at ~10:45 ET
railway logs --service aat-loop-hack<n>       # the heartbeat
```

`hack1` is attended: `AAT_ACCOUNT_ROLE=hack1 python -m scripts.competition_book`
after 15:45 ET, then MOC. Account numbers: hack1 PA3WXDS3MJ53 · hack2
PA33ON4NRJAX · hack3 PA3JYEG4DF9G · hack4 PA3R9XHMCVDA · hack5 PA3T8OTGULCD ·
hack6 PA3I816FLXE9 (`docs/seed/genesis_hack<n>.json`).

## What is NOT done

- Live proof of `convexity` and `council` mandates on a real account (needs the
  new keys). `council` needs `scripts.dislocation_scan --deep 3` run each
  morning on the day's printers or it holds cash — schedule it in the loop or
  by hand.
- `predator`'s pair leg remains gated by the 5pp MDM floor on breadth claims;
  it will trade shares on the continuation cell and refuse most pairs. Priced,
  not hidden.
- The $25 API credit: paste under its provider's `AAT_*` name; if it is a
  provider the council does not know (OpenAI), `alpha/council/providers.py`
  needs one entry.
