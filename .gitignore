__pycache__/
*.pyc
.env
.env.*
state/*.jsonl
.venv/
venv/
*.log

state/sec_cache/
state/loop_*.err
state/shadow_bars_*.json
state/belief_vs_chain/
state/night_shadow/bars_daily.json.gz
state/night_shadow/*.log
state/dashboard.html

# 2026-08-29: the PIT observation corpus is a regenerable data store (21 MB, 30k rows); receipts live in docs/
state/corpus/

# 2026-09-13: the allocator curves ARE the evidence (TRIAL-DRAFT-ALLOCATOR-v0).
# `state/*.jsonl` above would have swallowed them silently.
!state/allocator/curves/*.jsonl
