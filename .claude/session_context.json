{
  "hookSpecificOutput": {
    "hookEventName": "SessionStart",
    "additionalContext": "AEGIS ALPHA TERMINAL (paper trading, live broker credentials). READ AEGIS.md FIRST, then docs/canonical/00_NORTH_STAR.md, 01_SYSTEM_ARCHITECTURE.md, 02_ROADMAP.md, and 03_DAILY_INTELLIGENCE_LOOP.md. docs/canonical/CONTEXT_MANIFEST.yaml defines context/truth policy. Historical ROADMAP/HANDOFF/FINDING files are evidence/retrieval sources, not competing current intent. Then read docs/START_HERE_NEXT_SESSION.md for environment/tooling/provider state and retrieve only task-relevant historical findings. Strategy/models MAY adapt when evidence warrants it; never rewrite the data/model/version/hash/thesis contract of an existing prediction or position. Start with optimus session_briefing() + aegis_verified_state() when available. Invoke the task-observer skill for multi-step tasks. Run the suite with `python run_tests.py` and NOTHING else -- it sets AAT_TEST_MODE=1, which blocks sockets AND DNS in the suite and every child it spawns; running a tests_smoke_*.py file directly leaves the venue reachable. dev and exp1 are PRE_UNITS_FIX legacy books on --manage-only. A stopped loop reads exactly like a quiet market -- count agent_loop COMMAND LINES. An order skipping fills.py + ledger.py breaks the hash chain. Piping pytest into tail throws away its verdict."
  }
}
