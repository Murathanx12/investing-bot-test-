"""NARRATIVE SHOCK -- the gap between reality, public belief, attention and price.

Not sentiment. The commodity project of the week is `positive headline -> buy
call`. This package represents an event on SEPARATE axes so the LLM can say the
useful thing -- "probably false, highly market-moving" -- and the numeric
engine, not the LLM, decides whether the option chain has already charged for
it. See `schema.py` for the axes and `extract.py` for the extraction contract.
"""
